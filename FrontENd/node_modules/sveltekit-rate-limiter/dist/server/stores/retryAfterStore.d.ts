import type { RateLimiterStore } from './rateLimiterStore.js';
export declare class RetryAfterStore implements RateLimiterStore {
    private cache;
    constructor(maxItems?: number);
    clear(): Promise<void>;
    add(hash: string, ttl: number): Promise<number>;
}
