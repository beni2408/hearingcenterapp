import type { RateLimiterStore } from './rateLimiterStore.js';
export declare class TTLStore implements RateLimiterStore {
    private cache;
    constructor(maxTTL: number, maxItems?: number);
    clear(): Promise<void>;
    add(hash: string, ttl: number): Promise<number>;
    private set;
}
